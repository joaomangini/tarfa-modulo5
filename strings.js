let frase = "Javascript é legal em";

// Comprimento da String//
console.log(frase.length);

//Deixar em maiúsculo//
console.log(frase.toUpperCase());

//Deixar em minúsculo//
console.log(frasee.toLowerCase());

// Encontrar uma posição da string //
console.log(frase.index0f("legal"));

// Extrair uma parte da string //
console.log(frase.substring(0,10));

// Substituir uma parte da String //
let novafrase = frase.replace("legal", "incível");
console.log(novafrase);

//Interpolação de Strings//
let nome = "João Leonardo";
let idade = "25";

let mensagem = `Meu nome é ${nome} e eu tenho ${idade} anos.`;
console.log(mensagem);

